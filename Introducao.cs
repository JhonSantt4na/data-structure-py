public class EnemyAI : MonoBehaviour
{
   // Variaveis : Podemos mudar o valor em tempo de execução
   int health;
   float speed;
   string enemyName;
   bool idDead;

   // Comerça o metodo com o retorno 
   // void não precisa retornar nada

   void Movement()
   {  // && = e
      // || = Ou
      if (speed > 0 && health > 0)
      {

      }
   }
   void Dead()
   {
      if (health > 0)
      {

      }
      else
      {

      }
   }

   void Attack()
   {

   }
}
